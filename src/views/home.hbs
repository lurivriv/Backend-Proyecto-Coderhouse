<div class="app-content container">
    <h1 class="title-category">MENÚ</h1>
    {{#if productsNoFilter.length}}
        <div>
            <div class="item-list">
                {{#each productsNoFilter}}
                    <a href="/products/{{this._id}}" class="card-list">
                        <div class="card">
                            {{#if thumbnail}}
                                <img class="card-img-top" src="/assets/products/img/{{this.thumbnail}}" alt="{{this.title}}">
                            {{/if}}
                            <div class="card-body row justify-content-evenly">
                                <h5 class="card-title mb-3">{{this.title}}</h5>
                                <p class="col-auto text-card-list">$ {{this.price}}</p>
                                <p class="col-auto text-card-list category-card" data-category="{{this.category}}">
                                    {{this.category}}
                                </p>
                            </div>
                        </div>
                    </a>
                {{/each}}
            </div>
        </div>
    {{else}}
        <h2 class="title-category">No se pudo cargar el menú :(</h2>
    {{/if}}
</div>

<script type="module" src="/js/main.js"></script>